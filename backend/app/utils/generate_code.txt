import random
import string


def generate_guardian_code():

    prefix = "GUARD"

    suffix = "".join(
        random.choices(
            string.digits,
            k=6
        )
    )

    return f"{prefix}{suffix}"